import { employees } from '../gridOptions/employees'
import { productsListing } from '../gridOptions/productsListing'
import { searchClientsAndPatients } from '../gridOptions/searchClientsAndPatients'
import { tpv } from '../gridOptions/tpv'
import { pendingSell } from '../gridOptions/pendingSell'
import { modalAnnotations } from '../gridOptions/modalAnnotations'
import { sustitutions } from '../gridOptions/sustitutions'
import { audit } from '../gridOptions/audit'
import { warehouse } from '../gridOptions/warehouse'
import { shoppingOffers } from '../gridOptions/shoppingOffers'
import { lastShopping } from '../gridOptions/lastShopping'
import { customersListing } from '../gridOptions/customersListing'
import { labs } from '../gridOptions/labs'
import { labAddress } from '../gridOptions/labAddress'
import { labContacts } from '../gridOptions/labContacts'
import { entities } from '../gridOptions/entities'
import { dealings, history, addHistory } from '../gridOptions/boxes'
import { superfamilies, families, subfamilies } from '../gridOptions/families'
import { recipeBookModal } from '../gridOptions/recipeBook'
import { recipeBookHistory } from '../gridOptions/recipeBookHistory'

export const columns = {
  employees,
  tpv,
  searchClientsAndPatients,
  productsListing,
  modalAnnotations,
  sustitutions,
  audit,
  warehouse,
  shoppingOffers,
  lastShopping,
  customersListing,
  labs,
  pendingSell,
  labAddress,
  labContacts,
  entities,
  dealings,
  history,
  addHistory,
  superfamilies,
  families,
  subfamilies,
  recipeBookModal,
  recipeBookHistory,
}

export const icons = {
  menu: '<i class="fa fa-bath" style="width: 10px"/>',
  sortAscending: '<i class="fa fa-long-arrow-alt-down"/>',
  sortDescending: '<i class="fa fa-long-arrow-alt-up"/>',
}
