import styled from 'styled-components'

const InputContent = styled.div`
  height: 35px;
`
const Label = styled.div`
  font-size: 12px;
`

const Error = styled.div`
  font-size: 10px;
  height: 14px;
`
export { Label, InputContent, Error }
