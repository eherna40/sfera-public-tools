import React from 'react'
import { Icon } from './styles'

const IcCloseMD = ({ size, color }) => (
  <Icon className={`material-icons tw-text-${color}`} size={size}>
    close
  </Icon>
)

export default IcCloseMD
