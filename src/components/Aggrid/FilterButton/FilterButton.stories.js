import React from 'react'
import FilterButton from './FilterButton'

export default {
  title: 'components/aggrid/FilterButton',
  component: FilterButton,
}

const Template = (args) => <FilterButton {...args} />

export const Default = Template.bind({})
