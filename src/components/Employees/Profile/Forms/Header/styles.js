import styled from 'styled-components'

const Wrapper = styled.div`
  min-width: 154px;
  background-color: transparent;
`

const WrapperText = styled.span`
  font-size: 12px;
  font-weight: bold;
`

export { Wrapper, WrapperText }
