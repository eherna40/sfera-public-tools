import styled from 'styled-components'

const Wrapper = styled.div``

const WrapperIcon = styled.label`
  height: 100%;
  width: 100%;
`
const WrapperRemove = styled.div`
  font-size: 12px;
`

export const ContentModal = styled.div`
  height: 600px;
`

export { Wrapper, WrapperIcon, WrapperRemove }
